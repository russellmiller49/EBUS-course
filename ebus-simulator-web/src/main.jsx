import React from 'react';
import ReactDOM from 'react-dom/client';
import EBUSSimulator from '../../ebus-simulator.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <EBUSSimulator />
  </React.StrictMode>
);
